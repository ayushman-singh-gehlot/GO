package main

import "fmt"

func main() {
	var num int
	var fNum float64
	var name string                         // empty string
	var condition bool                      // false as default value
	fmt.Println(num, fNum, name, condition) // unlike other programming language it does to intialize variables with garbage values
	// instead use respective zero values for intializing
}
